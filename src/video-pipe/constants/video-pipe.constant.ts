/**
 * Historical default number of scenes for the pipeline; no longer enforced as a hard
 * requirement. The actual allowed range is MIN_SCENE_COUNT..MAX_SCENE_COUNT
 * (src/shared/constants/scene-count.ts).
 */
export const VIDEO_PIPE_SCENE_COUNT = 5;

export const VIDEO_PIPE_RESULT_KEY_PREFIX = 'videos/video-pipe';
